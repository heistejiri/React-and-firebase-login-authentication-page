import React from "react";

const Hero = () => {

  return(
      <section className="Hero">
        <nav>
          <h2>Welcome to Tejiri's Page</h2>
          <button>Logout</button>
        </nav>
      </section>
  );
};

export default Hero;